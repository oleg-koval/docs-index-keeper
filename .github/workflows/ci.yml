name: CI

on:
  push:
    branches: [main, beta]
  pull_request:
    branches: [main, beta]

permissions:
  contents: write
  pull-requests: write
  issues: write
  id-token: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - run: npm ci
      - run: npm run build
      - run: npm test

  release-readiness:
    needs: test
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/beta')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - run: npm ci
      - run: npm run build

      - name: Validate npm auth
        run: |
          if [ -z "$NPM_TOKEN" ]; then
            echo "::error::NPM_TOKEN secret is not configured."
            exit 1
          fi

          echo "//registry.npmjs.org/:_authToken=$(echo -n "$NPM_TOKEN" | tr -d '[:space:]')" > .npmrc
          npm whoami --registry https://registry.npmjs.org/ || (echo "::error::NPM_TOKEN invalid or expired. Create a new token at https://www.npmjs.com/settings/~/tokens (Granular, Read and write) and set NPM_TOKEN secret with no extra spaces/newlines." && exit 1)
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Validate release configuration
        run: npm run release:dry-run
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

  release:
    needs: [test, release-readiness]
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/beta')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - run: npm ci
      - run: npm run build

      # Use .npmrc with trimmed token (avoids 401 from trailing newline in secret)
      - name: Configure npm auth
        run: |
          echo "//registry.npmjs.org/:_authToken=$(echo -n "$NPM_TOKEN" | tr -d '[:space:]')" > .npmrc
          npm whoami --registry https://registry.npmjs.org/ || (echo "::error::NPM_TOKEN invalid or expired. Create a new token at https://www.npmjs.com/settings/~/tokens (Granular, Read and write) and set NPM_TOKEN secret with no extra spaces/newlines." && exit 1)
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Release
        run: npx semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
